자바 - 23.10.12_seq10_형변화응용

형변환 == 데이터타입 변환

1. 자동형변환
- 형변환	ex) byte(작은) --> int(큰)
- 자동으로 타입 바뀜(문제없음!)

2. 강제형변환
- 형변환	ex)int(큰) --> byte(작은)
- 강제로 cpu가 가지고 와서 앞에 0 자르고 ram에 집어 넣음
- 강제형변환을 할 때는 작은 공간에 들어갈 수 있는 값만 넣을 수 있다.

3. 참조형 형변환
- 참조형 형변환은 상속관계에서만 가능
- 가변길이, 타입이 여러 개일 때 사용
- list = {a, b, c}; (가변길이)
- list = {b, c}; 데이터 변경이 있을 때 자리를 옮김 - 자리 옮김
- 데이터 변경이 너무 많을 때는 사용이 불편(ram이 계속 일함)
- 때문에 ArrayList 권장

-


인터페이스 implements와 클래스 상속의 차이 알아보기

시험 나오려나

1. js(javascript)
> html - data, <>태그 사용 <a href="연결하고 싶은 페이지/주소">클릭</a>
> css - style, 여러개 html 페이지에 동일한 스타일 지정하고 싶은 외부에 out.css를 만들어서 	html에 연결해서 사용<link>
	1) css를 외부파일로 만들어 html에서 연결
	2) html안에 head태그 안에 <style>안에 css정의해서 사용
	3) 특정한 태그 안에 <button style="color:red"> style 속성을 사용

> javascript(js) - 자바와의 차이, 웹브라우저의 웹문서에 동적으로 처리 역할 담당
	1) js(브라우저에서 실행), 자바(서버에서 실행)
	2) js(함수중심), 자바(OOP)
	3) js(var(타입지정 x, 생략 가능), let, const), 자바(byte, short, int long, float, double, ...)
	4) js(함수), 자바(메서드)
	*외부연결<javascript>, html의 head <javascript>, 특정한 태드 안에 작성
	함수를 사용하고 싶으면 반드시 외부연결을 해야하는 것은 아님
	함수는 입력값 있는 것이 있고 없는 것이 있음
	함수는 반환값이 있는 것(return)이 있고 없는 것이 있음

> html안에 css, js를 여러개 넣을 있다. 여러개 쓸 수 있다. Html은 줄의 제약이 없습니다.
> 각 Html의 버튼, Input, table등의 태그에 스타일(css)을 지정할 수 있다.


2. Java(자바)
> 객체지향형프로그래밍(OOP, Object Oriented Programming), Java, C++ <--> 절차지향형프로그래밍, C
	클래스(틀) -> 클래스를 가지고 실제로 사용할 부품(객체, Object, 클래스의 실제, instance)
	하나의 클래스로는 여러개의 객체를 만들 수 있다.
> 데이터 타입
	- 기본형: 값이 저장, 정수, 실수, 문자1, 논리
	- 참조형: 주소가 저장, 기본형이 아닌 모두, 배열, 클래스, 인터페이스, 컬렉션, ...
> 연산자 - 산술(+ - * / % 등), 논리(&& || !), 비교(== != > < 등), 대입/할당(=), 연결(+)
> 배열 - 고정길이, 타입동일할 때 보통 많이 사용, length변수(개수) 읽기전용변수
> 컬렉션 - 가변길이, 타입 다를 때도 사용 가능
	- 순서: list, 중복 X: set, 키+값: map
> 클래스(틀) = 멤버변수(속성, 성질) + 멤버메서드(동작, 기능처리)
	- 메서드: 함수와 같은 개념
		입력값이 있을 수도 있고 없을 수도 있음. => 입력값이 있으면 매개변수 지정
		public int add(int x, int y) {	//x, y는 매개변수(파라미터)
			return x + y;}	// add(100, 200)
		//return이 없는 경우 public 뒤에는 void라고 써줌
		//return이 있는 경우 public 뒤에는 리턴하는 데이터 타입을 써줌

> 상속 - 클래스를 만들 때 기존에 있던 클래스를 재사용해서 좀 더 추가적으로 만들자라는 개념
	public class 하늘을 나는 자동차 extends 자동차()
	자동차(기존에 있던 클래스): 슈퍼클래스, 부모클래스
	하늘을 나느 자동차(새롭게 만든 클래스): 서브클래스, 자식클래스
	public C extends A, B {} => 여러개 상속 불가(다중상속 X)
	public C extends A {} => 하나의 부모로부터만 상속 가능(단일상속 O)

자바_형변환+컬렉션 피피티(101쪽)
문제풀이
1. (byte)
2. 상속
3. 부모클래스
4. 자식클래스
5. extends
6. 정수, 실수, 문자, 논리
7. 문자열, 배열, 컬렉션
8. t=(Truck)c;
9. 기본형 - 값, 참조형 - 주소
10. null
11. 아직 주소가 없다. (함수를 불러 쓸 수 없음)
12. Integer, Double
13. 자동형변환
14. 강제형변환





